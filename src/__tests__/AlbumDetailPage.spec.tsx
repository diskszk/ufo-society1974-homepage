export {};

describe("AlbumDetailPage", () => {
  describe("APIから取得したアルバムを表示する", () => {
    test.skip("アルバムのタイトルをヘッダーに表示する", () => {
      //
    });

    test.skip("アルバムの画像を表示する", () => {
      //
    });
    test.skip("説明事項がる場合、表示する", () => {
      //
    });
    test.skip("楽曲情報を3曲分表示する", () => {
      //
    });
    test.skip("楽曲ごとに曲名と小話を表示する", () => {
      //
    });
  });
});
